package com.study;

// ============================================
//  인터페이스 = 목차 / 규격
//  "이런 기능이 있다" 만 적는다. 어떻게 하는지는 안 적는다.
//  전자정부 소스의 ○○Service.java 가 전부 이 모양이다.
// ============================================
public interface MemberService {

    // 몸통 {} 이 없고 세미콜론(;) 으로 끝난다. 이게 인터페이스의 표시다.
    String greet(String name);
}
