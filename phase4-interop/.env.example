# 이 파일을 .env 로 복사해서 값을 채운다. .env 는 절대 커밋하지 않는다.
# 상대 기관이 발급해준 DB 계정. 반드시 '읽기 전용' 계정을 요구할 것.
PARTNER_DB_USER=여기에계정
PARTNER_DB_PASSWORD=여기에비밀번호
PARTNER_DB_NAME=여기에DB이름

# 전문 위·변조 방지용 공유 비밀키. 상대 기관과 공문/보안메일로 주고받는다.
# 절대 소스나 이 파일에 실제 값을 쓰지 않는다.
HMAC_SECRET=여기에공유비밀키
